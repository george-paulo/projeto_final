<!DOCTYPE html>
<html lang="pt-BR">
    <head>
        <meta charset="UTF-8">
    </head>
    <body>
        <%- include('/home/node/app/views/cabecalho.ejs') %>
        <h1><%= autor.nome %></h1>
        <section>
            <h1>Formações</h1>
            <ul>
                <% for (let formacao of autor.formacoes) { %>
                    <li><%= formacao %></li>
                <% } %>
            </ul>
        </section>
        <section>
            <h1>Experiências profissionais</h1>
            <ul>
                <% for (let experiencia of autor.experiencias) { %>
                    <li><%= experiencia %></li>
                <% } %>
            </ul>
        </section>
    </body>
</html>